Main

(proc main
    (print "Hello world")
    (var test true)
    (set test false)
    (if (== false (= test true))
        (print 340)
    )
    (else (if (== true test)
            (print "ok")
        )
    )
    (if (== false test)
        (print "hello")
    )
    (switch test (case false (print "hello")))
    (var hi (switch test (case false (yield 93)) (default (yield 123))))
)